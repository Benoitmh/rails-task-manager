<h1>My tasks</h1>

<ol>
  <% @tasks.each do |task| %>
    <li>
      <% task.completed %> | <%= link_to task.title, task_path(task) %> --- <%= task.details %> <%= link_to "🗑", task_path(task), data: { turbo_method: :delete, turbo_confirm: "Are you sure ?" } %>
    </li>
  <% end %>
</ol>
<%= link_to "Create task", "/tasks/new" %>
